{
    "name": "storix-monorepo",
    "version": "0.0.1",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "dev:backend": "npm run start:dev --workspace=backend",
        "build:backend": "npm run build --workspace=backend",
        "dev:web": "npm run dev --workspace=web"
    },
    "devDependencies": {
        "@types/node": "^25.0.3",
        "autoprefixer": "^10.4.23",
        "postcss": "^8.5.6",
        "tailwindcss": "^4.1.18"
    }
}
